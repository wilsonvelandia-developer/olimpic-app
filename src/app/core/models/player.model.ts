/**
 * Player models are defined in team.model.ts (same domain).
 * Re-exported here for backward compatibility with existing imports.
 */
export type {
  Player,
  PlayerCreateRequest,
  PlayerUpdateRequest,
} from './team.model';
