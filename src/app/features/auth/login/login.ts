import { Component, ChangeDetectionStrategy } from '@angular/core';

/** Placeholder — implementation in a future task. */
@Component({
  selector: 'app-login',
  template: `<h2>Iniciar Sesión</h2>`,
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class Login {}
